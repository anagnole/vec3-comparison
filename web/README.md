# Web frontend

This is a minimal Vite + React app that talks to the `api/` server.

Quick start:

```bash
cd web
npm install
npm run dev
```

The frontend expects the API at `http://localhost:3001` by default.
