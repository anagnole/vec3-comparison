React + Node UI quickstart

We provide a simple React frontend and a Node/Express API to run generate/ingest/docker commands from a browser.

1) Start API (from project root)

```bash
cd api
npm install
cd ..
source .venv/bin/activate
cd api
npm start
```

2) Start frontend

```bash
cd web
npm install
npm run dev
```
